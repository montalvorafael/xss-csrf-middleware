<!DOCTYPE html>
<html>
  <head>
    <title>Vulnerable Demo</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container mt-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card">
            <div class="card-header">
              <h2>Vulnerable Demo (Attacks Succeed)</h2>
            </div>
            <div class="card-body">
              <h1 class="alert alert-danger">
                Echo:
                <%- userInput %>
              </h1>
              <!-- XSS vuln: try ?userInput=<script>alert('XSS')</script> -->

              <form
                method="POST"
                action="/vulnerable/transfer"
                class="needs-validation"
              >
                <div class="mb-3">
                  <input
                    type="text"
                    class="form-control"
                    name="amount"
                    placeholder="Amount"
                    value="100"
                  />
                </div>
                <button type="submit" class="btn btn-danger">
                  Transfer (No CSRF check)
                </button>
              </form>

              <hr />

              <form
                method="POST"
                action="/vulnerable/change-password"
                class="needs-validation"
              >
                <div class="mb-3">
                  <input
                    type="text"
                    class="form-control"
                    name="newPassword"
                    placeholder="New Password"
                    value="secret123"
                  />
                </div>
                <button type="submit" class="btn btn-warning">
                  Change Password (No CSRF check)
                </button>
              </form>

              <!-- Inline JS runs freely -->
              <script>alert('Inline script runs!');</script>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
