<!DOCTYPE html>
<html>
  <head>
    <title>Protected Demo</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container mt-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card">
            <div class="card-header">
              <h2>Protected Demo (Attacks Fail)</h2>
            </div>
            <div class="card-body">
              <h1 class="alert alert-success">Echo: {{userInput}}</h1>
              <p class="text-muted">
                Here we do NOT render unescaped user input. We literally show
                the string "{{userInput}}" instead of injecting it.
              </p>

              <form
                method="POST"
                action="/protected/transfer"
                class="needs-validation"
              >
                <div class="mb-3">
                  <input
                    type="text"
                    class="form-control"
                    name="amount"
                    placeholder="Amount"
                    value="100"
                  />
                </div>
                <input
                  type="hidden"
                  name="csrfToken"
                  value="<%= csrfToken %>"
                />
                <button type="submit" class="btn btn-primary">
                  Transfer (CSRF protected)
                </button>
              </form>

              <hr />

              <form
                method="POST"
                action="/protected/change-password"
                class="needs-validation"
              >
                <div class="mb-3">
                  <input
                    type="text"
                    class="form-control"
                    name="newPassword"
                    placeholder="New Password"
                    value="secret123"
                  />
                </div>
                <input
                  type="hidden"
                  name="csrfToken"
                  value="<%= csrfToken %>"
                />
                <button type="submit" class="btn btn-secondary">
                  Change Password (CSRF protected)
                </button>
              </form>

              <!-- Safe inline script using nonce -->
              <script nonce="<%= nonce %>">
                console.log('Nonce-allowed script runs safely');
              </script>

              <!-- AJAX demo: send CSRF via header with fetch -->
              <script nonce="<%= nonce %>">
                const csrfToken = '<%= csrfToken %>';

                fetch('/protected/transfer', {
                  method: 'POST',
                  headers: {
                    'Content-Type': 'application/json',
                    'x-csrf-token': csrfToken
                  },
                  credentials: 'include',
                  body: JSON.stringify({ amount: 50 })
                })
                  .then(r => r.text())
                  .then(data => {
                    console.log('AJAX transfer success:', data);
                    document.body.innerHTML +=
                      '<div class="alert alert-info mt-3">AJAX Transfer: ' +
                      data +
                      '</div>';
                  })
                  .catch(err => console.error('AJAX failed:', err));
              </script>

              <!-- This SHOULD be blocked by CSP because it's missing nonce -->
              <script>
                alert('Inline without nonce - SHOULD BE BLOCKED BY CSP');
              </script>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
