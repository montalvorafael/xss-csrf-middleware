// index.js – main entry point for the npm package
module.exports = require('./middleware.js');

// If your actual middleware file has a different name (for example middleware/index.js, lib/middleware.js, etc.)
// just change the path above to match it exactly.
